pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..Tanmay'
            }
        }
        stage('Test') {
            steps {
                build 'java'
                echo "Resource Group Getting built!"
            }
        }
        stages {
    stage('Copy Archive') {
         steps {
             script {
                step ([$class: 'CopyArtifact',
                    projectName: 'arifacts',
                    filter: "packages/infra*.zip",
                    target: 'Infra']);
            }
        }
    }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
}
